# 🌍 Тестовое задание для IT школы Hello World

Простое Django-приложение с одной моделью, использующее PostgreSQL и развёрнутое в Docker-контейнерах.  
Проект демонстрирует базовую архитектуру современного Python-бэкенда по best practices.

---

## 📌 Что делает проект?

- Хранит сообщения в PostgreSQL через модель `Lesson`
- Предоставляет админку Django для управления уроками
- Автоматически применяет миграции при запуске
- Работает полностью в Docker (включая базу данных)

---

## 🚀 Как запустить?

### Требования

- Docker и Docker Compose

### 1. Создайте `.env` файл

Скопируйте пример:
```
DEBUG=True
SECRET_KEY=super-secret-DN*M!)@MN803djn1-key7578299
ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=db
POSTGRES_USER=root
POSTGRES_PASSWORD=123456
POSTGRES_HOST=db
POSTGRES_PORT=5432
```

### 2. Сделайте файл `entrypoint.sh` исполняемым:
`chmod +x entrypoint.sh`

### 3. Запуск:
```
docker compose build
docker compose up
```